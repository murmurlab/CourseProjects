#include "testing.h"

void run_test()
{
    printf("===========================TEST================================\n");
    char    *str = "$$";
    size_t  tmp;
    size_t  _;

    _ = 0;
    // tmp = var_name_len("aaa");
    // printf("var_name_len: %zu\n", tmp);
    printf("%d\n", "€");
    // printf("%d\n", (is_text(str[_]) && !((str[_] == '$') && is_var(str[_ + 1]))));
	exit(0);
}
